package com.logistics.design.config;

import org.springframework.context.annotation.Configuration;

/**
 * Knife4j配置
 * 
 * 注意：Knife4j 4.x版本已基于springdoc-openapi，
 * 配置已在application.yml中完成，
 * 此类仅作为标记使用
 */
@Configuration
public class Knife4jConfig {
    // 配置已在application.yml中完成
} 